아이디 변환 : 아이디의 글자에 맞는 위치를 가지고

16진수의 수를 넣어서 변환을 다시 하게 하는 방식이다.

소스 : 

        for(int z=1; z<=id.Length()-2; z++)//글자의 길이를 재서 그만큼 반복시킨다.
        {
            id2=id.SubString(z,1);//id2라는 변수에 글자한개를 집어넣는다.
            for(int x=0; x<28; x++)//영어의 정보를 확인하는 반복문
            {
                if(id2==english2[x])//영어의 정보 확인
                {
                    id=id+english[x];//id라는곳에 입력
                }
            }
        }